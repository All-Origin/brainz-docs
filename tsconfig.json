{
  // 👉 Not used in actual TS compilation, only for editor IntelliSense
  "extends": "@docusaurus/tsconfig", // inherits from Docusaurus defaults

  "compilerOptions": {
    "baseUrl": "." // enables path resolution relative to the project root
  },

  "exclude": [
    ".docusaurus", // ignore generated files
    "build"        // ignore build output
  ]
}
// This file is used to configure TypeScript for Docusaurus projects.
// It extends the default Docusaurus TypeScript configuration and allows for customizations.  